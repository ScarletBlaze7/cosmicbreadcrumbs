/**
 * Celestial Fireworks & Pyrotechnic Celebrations (Disabled)
 * Replaced with elegant 3D card flips on draw/pull.
 */

import {
  triggerFireworks,
  triggerFireworksFromClick,
  triggerFireworkBurst,
  FireworkOptions,
} from './shootingStarShower';

export {
  triggerFireworks,
  triggerFireworksFromClick,
  triggerFireworkBurst,
  type FireworkOptions,
};

export default triggerFireworks;
