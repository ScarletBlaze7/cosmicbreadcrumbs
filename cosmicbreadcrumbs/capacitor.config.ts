import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cosmicbreadcrumbs.app',
  appName: 'Cosmic Breadcrumbs',
  webDir: 'dist'
};

export default config;
